const LOGIN_FAILURE = 'FAILURE';
const LOGIN_SUCCESS = 'SUCCESS';
const LOGIN_IN_PROGRESS = 'IN_PROGRESS';

const loginConstants = {
  LOGIN_FAILURE,
  LOGIN_SUCCESS,
  LOGIN_IN_PROGRESS,
};

export default loginConstants;
